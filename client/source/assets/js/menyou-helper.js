
var Locu = {};

Locu.getToken(username, password, callback) {
  // POST /api/token
};

Locu.register(username, password, callback) {
  // POST /api/register
};

Locu.validateToken(callback) {
  // GET /api/validate
};

Locu.getDishes(callback) {
  // GET /api/dishes/
};

Locu.getTasteProfile(callback) {
  // GET /api/taste/
};

Locu.updateTesteProfile(add, remove, callback) {
  // PUT /api/taste/
};
